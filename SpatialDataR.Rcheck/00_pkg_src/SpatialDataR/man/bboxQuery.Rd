% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-query.R
\name{bboxQuery}
\alias{bboxQuery}
\alias{bboxQuery,DataFrame-method}
\alias{bboxQuery,SpatialData-method}
\title{Bounding box spatial query}
\usage{
bboxQuery(x, xmin, xmax, ymin, ymax)

\S4method{bboxQuery}{DataFrame}(x, xmin, xmax, ymin, ymax)

\S4method{bboxQuery}{SpatialData}(x, xmin, xmax, ymin, ymax)
}
\arguments{
\item{x}{A \code{\linkS4class{SpatialData}} object, a
\code{DataFrame} with \code{x} and \code{y} columns, or a
\code{SimpleList} of such elements.}

\item{xmin}{Numeric. Minimum x coordinate.}

\item{xmax}{Numeric. Maximum x coordinate.}

\item{ymin}{Numeric. Minimum y coordinate.}

\item{ymax}{Numeric. Maximum y coordinate.}
}
\value{
Same class as input, subsetted to the bounding box.
}
\description{
Subsets a \code{SpatialData} object or individual elements
to a rectangular region of interest. For point/shape
\code{DataFrame} elements, rows outside the bounding box are
removed. For image/label path references, the bounding box is
stored as metadata (load and crop manually with
\code{readZarrArray}).
}
\details{
This mirrors the \code{bounding_box_query()} function from
the Python \code{spatialdata} library.
}
\examples{
library(S4Vectors)
pts <- DataFrame(
    x = c(1, 2, 3, 4, 5),
    y = c(5, 4, 3, 2, 1),
    gene = c("A", "B", "C", "D", "E")
)
# Query points in [2,4] x [2,4]
sub <- bboxQuery(pts, xmin = 2, xmax = 4, ymin = 2, ymax = 4)
sub
}
\references{
Marconato L et al. (2024). SpatialData: an open and universal
data framework for spatial omics. \emph{Nat Methods} 21:2196-2209.
\doi{10.1038/s41592-024-02212-x}
}
