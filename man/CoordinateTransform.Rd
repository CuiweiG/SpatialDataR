% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transforms.R
\name{CoordinateTransform}
\alias{CoordinateTransform}
\title{Create a CoordinateTransform}
\usage{
CoordinateTransform(
  type = c("identity", "affine"),
  affine = diag(3),
  input_cs = "global",
  output_cs = "global"
)
}
\arguments{
\item{type}{Character. \code{"identity"} or \code{"affine"}.}

\item{affine}{Numeric matrix. 3x3 for 2D or 4x4 for 3D affine.
Ignored for identity transforms.}

\item{input_cs}{Character. Input coordinate system name.}

\item{output_cs}{Character. Output coordinate system name.}
}
\value{
A \code{\link{CoordinateTransform}} object.
}
\description{
Constructs an affine or identity coordinate transformation.
Supports 2D (3x3 matrix) and 3D (4x4 matrix) transforms.
}
\examples{
# Identity transform
ct <- CoordinateTransform("identity")

# 2D scale + translate
mat <- matrix(c(0.5, 0, 10, 0, 0.5, 20, 0, 0, 1),
    nrow = 3, byrow = TRUE)
ct <- CoordinateTransform("affine", affine = mat,
    input_cs = "pixels", output_cs = "microns")
ct

# 3D affine (4x4)
ct3d <- CoordinateTransform("affine", affine = diag(4) * 0.5)
}
\references{
OME-NGFF coordinate transformations specification.
\url{https://ngff.openmicroscopy.org/latest/}
}
\seealso{
\code{\link{composeTransforms}} for chaining,
  \code{\link{invertTransform}} for inversion.
}
