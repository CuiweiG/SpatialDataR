% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plotSpatialData}
\alias{plotSpatialData}
\title{Plot SpatialData elements with optional image overlay}
\usage{
plotSpatialData(
  sdata,
  points = NULL,
  shapes = NULL,
  image = NULL,
  color_by = NULL,
  point_size = 0.3,
  point_alpha = 0.5,
  shape_fill = "transparent",
  shape_color = "red",
  max_points = 500000L,
  ...
)
}
\arguments{
\item{sdata}{A \code{\linkS4class{SpatialData}} object.}

\item{points}{Character or NULL. Name of a points element
to plot (from \code{spatialPoints(sdata)}).}

\item{shapes}{Character or NULL. Name of a shapes element
to plot (from \code{shapes(sdata)}).}

\item{image}{Character or NULL. Name of an image element
to use as background (from \code{images(sdata)}).}

\item{color_by}{Character or NULL. Column name in the
points data to map to color.}

\item{point_size}{Numeric. Size for \code{geom_point}
(default 0.3).}

\item{point_alpha}{Numeric. Alpha for points (default 0.5).}

\item{shape_fill}{Character. Fill color for polygon shapes
(default \code{"transparent"}).}

\item{shape_color}{Character. Border color for polygon
shapes (default \code{"red"}).}

\item{max_points}{Integer. Maximum number of points to
plot (subsampled if exceeded). Default 500000.}

\item{...}{Additional parameters (currently unused).}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates a \pkg{ggplot2} visualization of SpatialData elements.
Supports image backgrounds, point overlays (transcripts or
point-type shapes), and polygon shapes.
}
\examples{
## plotSpatialData(sd, points = "transcripts",
##     image = "morphology_focus")
}
